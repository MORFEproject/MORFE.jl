# Disabled: MORFE.jl uses Documenter.jl (Documentation.yml) to build and
# deploy its docs.  Re-enabling this Jekyll workflow would conflict with that
# deployment because both write to the same GitHub Pages environment.
# Update Documentation.yml and docs/make.jl if you want to change the toolchain.

name: "(disabled) Jekyll Pages"

# Only runs when manually triggered from the Actions tab — effectively a no-op.
on:
  workflow_dispatch:

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: "true"

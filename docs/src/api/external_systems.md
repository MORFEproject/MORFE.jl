# ExternalSystems

Represents autonomous polynomial dynamical systems used as external forcing: `dr/dt = f(r)`.
Stores the full polynomial dynamics, the linear matrix, and its eigenvalues.

```@autodocs
Modules = [MORFE.ExternalSystems]
Order   = [:type, :function]
Private = true
```

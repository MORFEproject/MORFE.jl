# Multiindices

Multi-index sets (exponent vectors) with graded lexicographic (Grlex) ordering.
Provides sorted containers, efficient generation, queries, factorizations, and combinatorial counting.

```@autodocs
Modules = [MORFE.Multiindices]
Order   = [:type, :function]
Private = true
```

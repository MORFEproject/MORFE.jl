# FullOrderModel

Types for full-order mechanical systems: `NDOrderModel` (N-th order ODE) and `FirstOrderModel`
(first-order form). Both support multilinear nonlinear terms and polynomial external forcing.

```@autodocs
Modules = [MORFE.FullOrderModel]
Order   = [:type, :function]
Private = true
```

# Eigenproblems

Eigenproblem assembly, sorting, and master-mode selection utilities.
Wraps the generalised eigenproblem and provides helpers for choosing the master subspace.

```@autodocs
Modules = [MORFE.Eigenproblems]
Order   = [:type, :function]
Private = true
```

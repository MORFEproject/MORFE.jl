# MultilinearMaps

Multilinear maps for representing polynomial nonlinear terms in full-order models.
Each map stores an in-place function, a multiindex encoding which derivative-order slots it consumes, and an external forcing multiplicity.

```@autodocs
Modules = [MORFE.MultilinearMaps]
Order   = [:type, :function]
Private = true
```

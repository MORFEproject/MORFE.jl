# Realification

Transforms complex-valued multivariate polynomials into equivalent real-valued polynomials.
Useful when complex normal coordinates appear in dynamical systems but the final equations must be expressed in real variables.

```@autodocs
Modules = [MORFE.Realification]
Order   = [:type, :function]
Private = true
```

# Eigensolvers

ARPACK-based generalised eigensolvers with shift-and-invert for large sparse systems.
Returns eigenpairs sorted by proximity to a target shift.

```@autodocs
Modules = [MORFE.Eigensolvers]
Order   = [:type, :function]
Private = true
```

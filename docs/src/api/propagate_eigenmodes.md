# PropagateEigenmodes

Left and right eigenvector propagation for N-th order systems.
Converts eigenpairs from the first-order companion form back to the original N-th order representation.

```@autodocs
Modules = [MORFE.PropagateEigenmodes]
Order   = [:type, :function]
Private = true
```
